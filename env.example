# Klaunx AI License Server Configuration

# Server Configuration
PORT=3000
NODE_ENV=production

# Security
ADMIN_KEY=your_super_secret_admin_key_here_change_this
JWT_SECRET=your_jwt_secret_key_for_tokens

# CORS Configuration (comma-separated)
ALLOWED_ORIGINS=https://klaunx.com,https://www.klaunx.com

# Database (SQLite by default, can be changed to PostgreSQL/MySQL)
DATABASE_URL=./licenses.db

# Optional: Email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Webhook for Gumroad integration
GUMROAD_WEBHOOK_SECRET=your_gumroad_webhook_secret
